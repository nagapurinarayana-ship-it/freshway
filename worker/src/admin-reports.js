import { json, n, rangeBounds } from './admin-shared.js';

export async function summary(env){const r=await env.DB.batch([env.DB.prepare("SELECT COUNT(*) count FROM orders WHERE delivery_status NOT IN ('Cancelled','Delivered')"),env.DB.prepare("SELECT COUNT(*) count FROM orders WHERE delivery_status IN ('New','Ordered')"),env.DB.prepare("SELECT COUNT(*) count FROM orders WHERE delivery_status='Processing'"),env.DB.prepare("SELECT COUNT(*) count FROM orders WHERE delivery_status='Delivered'"),env.DB.prepare("SELECT COUNT(*) count FROM orders WHERE delivery_status NOT IN ('Delivered','Cancelled') AND delivery_plan='Today'"),env.DB.prepare("SELECT COALESCE(SUM(total),0) amount FROM orders WHERE payment_status='Not Collected' AND delivery_status!='Cancelled'"),env.DB.prepare('SELECT COUNT(*) count FROM customers'),env.DB.prepare('SELECT COUNT(*) count FROM products WHERE active=1'),env.DB.prepare('SELECT COUNT(*) count FROM products')]);const v=r.map(x=>x.results?.[0]||{});return{newOrders:Number(v[1].count||0),processing:Number(v[2].count||0),pendingDelivery:Number(v[0].count||0),delivered:Number(v[3].count||0),deliveryToday:Number(v[4].count||0),cashPending:Number(v[5].amount||0),customers:Number(v[6].count||0),activeProducts:Number(v[7].count||0),products:Number(v[8].count||0)}}

export async function reports(env,u){const b=rangeBounds(u),p=[b.start,b.end];const q=await env.DB.batch([env.DB.prepare("SELECT COUNT(*) count,COALESCE(SUM(CASE WHEN delivery_status!='Cancelled' AND payment_status!='Cancelled' THEN total ELSE 0 END),0) sales,COALESCE(SUM(CASE WHEN delivery_status='Delivered' AND payment_status!='Cancelled' THEN total ELSE 0 END),0) delivered_sales,COALESCE(SUM(CASE WHEN delivery_status='Cancelled' THEN total ELSE 0 END),0) cancelled_value,COUNT(DISTINCT customer_id) customers FROM orders WHERE created_at>=? AND created_at<=?").bind(...p),env.DB.prepare("SELECT COUNT(*) count,COALESCE(SUM(total),0) amount FROM orders WHERE created_at>=? AND created_at<=? AND payment_status='Not Collected' AND delivery_status!='Cancelled'").bind(...p),env.DB.prepare("SELECT COUNT(*) count,COALESCE(SUM(total),0) amount FROM orders WHERE datetime(payment_collected_at)>=datetime(?) AND datetime(payment_collected_at)<=datetime(?) AND payment_status='Collected'").bind(...p),env.DB.prepare("SELECT COUNT(*) count FROM orders WHERE created_at>=? AND created_at<=? AND delivery_status='Delivered'").bind(...p),env.DB.prepare("SELECT COUNT(*) count FROM orders WHERE created_at>=? AND created_at<=? AND delivery_status='Cancelled'").bind(...p),env.DB.prepare("SELECT COUNT(DISTINCT o.customer_id) count FROM orders o WHERE o.created_at>=? AND o.created_at<=? AND NOT EXISTS(SELECT 1 FROM orders old WHERE old.customer_id=o.customer_id AND old.created_at<?)").bind(b.start,b.end,b.start),env.DB.prepare("SELECT COUNT(DISTINCT o.customer_id) count,COUNT(*) orders FROM orders o WHERE o.created_at>=? AND o.created_at<=? AND EXISTS(SELECT 1 FROM orders old WHERE old.customer_id=o.customer_id AND old.created_at<?)").bind(b.start,b.end,b.start),env.DB.prepare("SELECT oi.name,oi.unit,SUM(oi.qty) qty,SUM(oi.line_total) value FROM order_items oi JOIN orders o ON o.id=oi.order_id WHERE o.created_at>=? AND o.created_at<=? AND o.delivery_status!='Cancelled' AND o.payment_status!='Cancelled' GROUP BY oi.product_id,oi.name,oi.unit ORDER BY value DESC LIMIT 5").bind(...p),env.DB.prepare("SELECT o.customer_name,COUNT(*) orders,SUM(o.total) value FROM orders o WHERE o.created_at>=? AND o.created_at<=? AND o.delivery_status!='Cancelled' AND o.payment_status!='Cancelled' GROUP BY o.customer_id,o.customer_name ORDER BY value DESC LIMIT 5").bind(...p)]);const v=q.map(x=>x.results?.[0]||{}),base=v[0],sales=Number(base.sales||0),cancelled=Number(base.cancelled_value||0);return{range:b.label,from:b.start,to:b.end,orders:Number(base.count||0),sales,deliveredSales:Number(base.delivered_sales||0),cancelledValue:cancelled,customers:Number(base.customers||0),aov:Number(base.count?sales/Math.max(1,Number(base.count)-Number(v[4].count||0)):0),cashPending:Number(v[1].amount||0),cashPendingOrders:Number(v[1].count||0),cashCollected:Number(v[2].amount||0),cashCollectedOrders:Number(v[2].count||0),delivered:Number(v[3].count||0),cancelled:Number(v[4].count||0),newCustomers:Number(v[5].count||0),repeatCustomers:Number(v[6].count||0),repeatOrders:Number(v[6].orders||0),topProducts:(q[7].results||[]).map(x=>({name:x.name,unit:x.unit,qty:Number(x.qty||0),value:Number(x.value||0)})),topCustomers:(q[8].results||[]).map(x=>({name:x.customer_name,orders:Number(x.orders||0),value:Number(x.value||0)}))}}